import { SectionTitle, SectionWrapper } from '../../../utils/utils'

const Finance = () => {
    return (
        <SectionWrapper>
            <SectionTitle>
                Финансы
            </SectionTitle>
        </SectionWrapper>
    )
}

export default Finance